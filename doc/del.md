```js

db.delete("t_user").                      OrmDel  == by ;Exec(返回行数+err);  Err(返回err)
   
    byModel(user)
    byMap(map)
    byPrimaryKeys(any)
    filterPrimaryKeys(any)
    byWhere(*whereBuider)

    .exec() //返回受影响的行数 num,err


```